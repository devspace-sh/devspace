version: v2beta1
name: docker-compose

images:
  foo:
    image: foo
    dockerfile: Dockerfile
    buildArgs:
      buildno: 1
      gitcommithash: cdc3b19

deployments:
  foo:
    helm:
      chart:
        name: component-chart
        repo: https://charts.devspace.sh
      values:
        containers:
        - name: foo-container
          image: foo
