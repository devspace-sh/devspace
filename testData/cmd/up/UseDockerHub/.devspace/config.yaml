cluster:
  cloudProvider: devspace-cloud
  cloudProviderDeployTarget: production
devSpace:
  deployments:
  - helm:
      chartPath: ./chart
      devOverwrite: ./chart/dev-overwrite.yaml
    name: devspace-default
  ports:
  - portMappings:
    - localPort: 3000
      remotePort: 3000
    - localPort: 9229
      remotePort: 9229
    service: default
  services:
  - labelSelector:
      devspace: default
    name: default
  sync:
  - containerPath: /app
    localSubPath: ./
    service: default
    uploadExcludePaths:
    - Dockerfile
    - .devspace/
    - chart/
    - node_modules/
images:
  default:
    name: floriankutz/devspace
version: v1alpha1
