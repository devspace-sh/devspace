The function `sleep` takes in the number of seconds for a process to wait before continuing.

## Arguments

### [seconds]

```
sleep 5   # process waits for 5 seconds
```

### Example

Use `sleep` as a function under your pipeline definitions:

```yaml
pipelines:
  dev:
    run: |-
      sleep 5
```
