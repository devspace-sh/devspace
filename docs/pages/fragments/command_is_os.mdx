The function `is_os` checks if a given argument is the current runtime GOOS.

## Arguments

### `[operatingSystemString]`

```
is_os [os]
```

### Example

TODO: Fix example

```yaml
pipelines:
  dev:
    run: |-
      is_os
```
