The function `is_command` checks if a given argument is a command in DevSpace.

## Arguments

### `[command]`

```
is_command [command]
```

### Example

TODO: Fix example

```yaml
pipelines:
  dev:
    run: |-
      is_command
```
