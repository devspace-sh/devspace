The following command is useful to verify that the profile modifies the config as intended:
```bash
devspace print -p my-profile
```
The above command would print the config after applying the all profile `patches` and `replace` statements as well as after replacing all config variables.
