version: v2beta1
name: build
images:
  app:
    image: username/app
    tags:
    - latest
    build:
      kaniko:
        cache: false
        args:
        - --no-push
pipelines:
    build:
        name: build
        steps:
            - run: build_images app
