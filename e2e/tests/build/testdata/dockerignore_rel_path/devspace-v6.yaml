version: v2beta1
name: build
images:
  app:
    image: username/app
    tags:
    - latest
    dockerfile: app/Dockerfile.dev
    context: app/
pipelines:
    build:
        name: build
        steps:
            - run: build_images app
