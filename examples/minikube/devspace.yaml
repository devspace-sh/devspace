version: v1beta3
images:
  default:
    image: devspace
    tag: latest
deployments:
- name: default
  helm:
    chart:
      name: ./chart
dev:
  ports:
  - imageName: default
    forward:
    - port: 3000
  sync:
  - imageName: default
    excludePaths:
    - node_modules
