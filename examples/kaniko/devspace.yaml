version: v1beta3
images:
  default:
    image: devspacecloud/devspace
    build:
      kaniko:
        cache: true
deployments:
  - name: devspace-default
    helm:
      chart:
        name: ./chart
dev:
  interactive:
    images:
    - name: default
  ports:
    - labelSelector:
        app.kubernetes.io/component: default
      forward:
        - port: 8080
  sync:
    - labelSelector:
        app.kubernetes.io/component: default
