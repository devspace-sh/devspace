version: v1beta2
images:
  default:
    image: devspace
    build:
      docker:
        skipPush: true
dependencies:
  - source:
      path: ../dependency2
deployments:
  - name: dependency1
    component:
      containers:
        - image: devspace
