version: v1beta2
deployments:
  - name: root
    component:
      containers:
        - image: nginx
dependencies:
  - source:
      path: dependency1
  - source:
      path: dependency2
cluster:
  kubeContext: minikube
  namespace: devspace
