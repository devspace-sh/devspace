version: v1beta3
deployments:
  - name: swag
    component:
      containers:
        - name: test
          image: dscr.io/${DEVSPACE_USERNAME}/test
      options:
        tillerNamespace: ${tiller}
commands:
  - name: test
    command: "echo ${test}"
